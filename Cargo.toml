[package]
name = "text-editor"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.13.1", features = ["tokio", "highlighter"] }
rfd = "0.16.0"
tokio = { version = "1.48.0", features = ["fs", "rt"] }
